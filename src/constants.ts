/**
 * Constants for Transactional Better Auth plugin
 */

/** Default provider ID */
export const DEFAULT_PROVIDER_ID = "transactional";

/** Default OAuth scopes */
export const DEFAULT_SCOPES = ["openid", "profile", "email"];

/** OIDC discovery path */
export const DISCOVERY_PATH = "/.well-known/openid-configuration";
